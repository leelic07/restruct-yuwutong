/**
 * Created by Administrator on 2018/3/15/015.
 */
import store from '@/store'

describe('Families.vue', () => {
  //分页查询家属信息的异步请求
  it('获取家属信息的异步请求', done => {
    // store.dispatch('getFamilies', {
    //   prison: '4501',
    //   username: '4501_sh',
    //   password: '123456'
    // }).then(res => {
    //   expect(res.code).to.be.an(200);
    //   done();
    // }).catch(err => console.log(err));
    done();
  });
});
